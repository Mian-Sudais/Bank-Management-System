<Window
    x:Class="ModernUI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Bank Management System">

    <Window.SystemBackdrop>
        <DesktopAcrylicBackdrop />
    </Window.SystemBackdrop>

    <Grid Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- ✅ Modern Custom Title Bar -->
        <Grid x:ConnectionId='2' x:Name="AppTitleBar" Height="48" Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition x:ConnectionId='4' x:Name="LeftPaddingColumn" Width="0"/>
                <ColumnDefinition x:ConnectionId='5' x:Name="IconColumn" Width="Auto"/>
                <ColumnDefinition x:ConnectionId='6' x:Name="TitleColumn" Width="Auto"/>
                <ColumnDefinition x:ConnectionId='7' x:Name="DragColumn" Width="*"/>
                <ColumnDefinition x:ConnectionId='8' x:Name="RightPaddingColumn" Width="0"/>
            </Grid.ColumnDefinitions>

            <!-- App Icon -->
            <Image x:ConnectionId='9' x:Name="TitleBarIcon" 
                   Source="ms-appx:///Assets/app.ico"
                   Grid.Column="1"
                   Width="16" Height="16"
                   Margin="12,0,8,0"/>

            <!-- App Title -->
            <TextBlock x:ConnectionId='10' x:Name="TitleBarText"
                       Text="Bank Management System"
                       Style="{StaticResource CaptionTextBlockStyle}"
                       Grid.Column="2"
                       VerticalAlignment="Center"
                       FontWeight="SemiBold"/>
        </Grid>

        <!-- Main Content Frame -->
        <Frame x:ConnectionId='3' x:Name="MainFrame" Grid.Row="1">
            <Frame.ContentTransitions>
                <TransitionCollection>
                    <EntranceThemeTransition IsStaggeringEnabled="True"/>
                    <RepositionThemeTransition IsStaggeringEnabled="False"/>
                </TransitionCollection>
            </Frame.ContentTransitions>
        </Frame>
    </Grid>
</Window>

